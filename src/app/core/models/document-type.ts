export class DocumentType {
    docTypeId!: number;
    documentType!: string;
  
    constructor(docTypeId: number, documentType: string) {
      this.docTypeId = docTypeId;
      this.documentType = documentType;
    }
  }
  